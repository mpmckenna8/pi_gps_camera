// a file mostly to help with what happens with all the event listener things

function changeRotate(settings, rotate_input ) {

  settings.rotateAngle = rotate_input.value;

  console.log('need to do the rotate thing', settings)


  return 0;
}
